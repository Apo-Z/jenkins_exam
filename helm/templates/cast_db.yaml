apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-{{ .Values.cast-db.name }}
spec:
  accessModes:
    - {{ .Values.cast-db.pvc.accessMode }}
  storageClassName: {{ .Values.cast-db.pvc.storageClassName }}
  resources:
    requests:
      storage: {{ .Values.cast-db.pvc.storage }}

---

apiVersion: v1
kind: Secret
metadata:
  name: cast-db
type: Opaque
data:
  POSTGRES_DB: {{ .Values.cast-db.secret.db | b64enc }}
  POSTGRES_USER: {{ .Values.cast-db.secret.username | b64enc }}
  POSTGRES_PASSWORD: {{ .Values.cast-db.secret.password | b64enc }}

---

apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: hpa-{{ .Values.cast-db.name }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: {{ .Values.cast-db.deployment.kind }}
    name: {{ .Values.cast-db.name }}
  minReplicas: {{ .Values.cast-db.hpa.minReplicas }}
  maxReplicas: {{ .Values.cast-db.hpa.maxReplicas }}
  targetCPUUtilizationPercentage: {{ .Values.cast-db.hpa.targetCPUUtilizationPercentage }}

---

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.cast-db.name }}
spec:
  type: {{ .Values.cast-db.service.type }}
  selector:
    app: {{ .Values.cast-db.name }}
  ports:
    - protocol: {{ .Values.cast-db.service.protocol }}
      port: {{ .Values.cast-db.service.port }}
      targetPort: {{ .Values.cast-db.service.targetPort }}
      nodePort: {{ .Values.cast-db.service.nodePort }} 

---

apiVersion: apps/v1
kind: {{ .Values.cast-db.deployment.kind }}
metadata:
  name: {{ .Values.cast-db.name }}
  labels:
    app: {{ .Values.cast-db.name }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.cast-db.name }}
  replicas: {{ .Values.cast-db.deployment.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ .Values.cast-db.name }}
    spec:
      containers:
        - name: {{ .Values.cast-db.deployment.name }}
          image: "{{ .Values.cast-db.deployment.image.repository }}:{{ .Values.cast-db.deployment.image.tag | default .Values.imageTag }}"
          ports:
            - containerPort: {{ .Values.cast-db.deployment.ports }}
          volumeMounts:
            - name: db
              mountPath: /var/lib/postgresql/data/
          envFrom:
            - secretRef:
                name: {{ .Values.cast-db.name }}
      volumes:
        - name: db
          persistentVolumeClaim:
            claimName: pvc-{{ .Values.cast-db.name }}

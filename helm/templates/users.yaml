apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-{{ .Values.users.name }}
spec:
  accessModes:
    - {{ .Values.users.pvc.accessMode }}
  storageClassName: {{ .Values.users.pvc.storageClassName }}
  resources:
    requests:
      storage: {{ .Values.users.pvc.storage }}


---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
 name: hpa-{{ .Values.users.name }}
 namespace: {{ .Values.namespace }}
spec:
 scaleTargetRef:
   apiVersion: apps/v1
   kind: {{ .Values.users.deployment.kind }}
   name: {{ .Values.users.name }}
 minReplicas: {{ .Values.users.hpa.minReplicas }}
 maxReplicas: {{ .Values.users.hpa.maxReplicas }}
 targetCPUUtilizationPercentage: {{ .Values.users.hpa.targetCPUUtilizationPercentage }}

---

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.users.name }}
spec:
  selector:
    app: {{ .Values.users.name }}
  ports:
    - protocol: {{ .Values.users.service.protocol }}
      port: {{ .Values.users.service.port }}
      targetPort: {{ .Values.users.deployment.ports }}

---

apiVersion: apps/v1
kind: {{ .Values.users.deployment.kind }}
metadata:
  name: {{ .Values.users.name }}
  labels:
    app: {{ .Values.users.name }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.users.name }}
  replicas: {{ .Values.users.deployment.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ .Values.users.name }}
    spec:
      containers:
        - name: {{ .Values.users.deployment.name }}
          image: "{{ .Values.users.deployment.image.repository }}:{{ .Values.users.deployment.image.tag | default .Values.imageTag }}"
          ports:
            - containerPort: {{ .Values.users.deployment.ports }}